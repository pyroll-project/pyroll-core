from ..base import GrooveBase


class DiamondGrooveBase(GrooveBase):
    """Base class for diamond like grooves.
    Does not provide any additional functionality to GrooveBase, but can be used as marker class."""
